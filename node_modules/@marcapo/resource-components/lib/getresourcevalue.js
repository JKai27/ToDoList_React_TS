import { useEffect, useState } from "react";
import { ResourceStore } from "./resourcestore";
export var GetResourceValue = function (resourceKey, context) {
    var _a = useState(""), val = _a[0], setVal = _a[1];
    useEffect(function () {
        void ResourceStore.getResourceValue(resourceKey, context).then(function (resolvedVal) { return setVal(resolvedVal); });
    }, [resourceKey]);
    return val;
};
