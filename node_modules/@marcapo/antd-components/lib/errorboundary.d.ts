import React, { ErrorInfo, ReactNode } from "react";
interface IErrorBoundary {
    forwardToModuleError?: boolean;
    fallback?: ReactNode;
    children?: ReactNode;
}
interface IState {
    hasError: boolean;
    stack?: string;
    error?: string;
    fallback: ReactNode;
}
export declare class ErrorBoundary extends React.Component<IErrorBoundary> {
    state: IState;
    constructor(props: IErrorBoundary);
    static getDerivedStateFromError(): Partial<IState>;
    render(): ReactNode;
    componentDidCatch(error: Error, errorInfo: ErrorInfo): void;
}
export {};
