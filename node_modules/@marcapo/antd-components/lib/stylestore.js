var StyleStoreInstance = /** @class */ (function () {
    function StyleStoreInstance() {
        this.cachedStyles = [];
    }
    StyleStoreInstance.prototype.getStyleValueByCssClassName = function (cssClassName, attribute) {
        var value = "";
        var cachedStyle = this.cachedStyles.find(function (style) { return style.cssClassName === cssClassName && style.attribute === attribute; });
        if (!cachedStyle) {
            var dummyDiv = document.createRange().createContextualFragment("<div id='dummyDiv' class=\"".concat(cssClassName, "\" />"));
            document.body.appendChild(dummyDiv);
            var div = document.querySelector("#dummyDiv");
            if (div) {
                var style = window.getComputedStyle(div);
                value = style[attribute];
                var newCachedStyle = {};
                newCachedStyle.attribute = attribute;
                newCachedStyle.value = value;
                newCachedStyle.cssClassName = cssClassName;
                this.cachedStyles.push(newCachedStyle);
                if (div.parentNode) {
                    div.parentNode.removeChild(div);
                }
            }
        }
        else {
            value = cachedStyle.value;
        }
        return value;
    };
    return StyleStoreInstance;
}());
export var StyleStore = new StyleStoreInstance();
//# sourceMappingURL=stylestore.js.map